#include <bits/stdc++.h>
using namespace std;

int add(const string &numbers)
{
    if (numbers.empty())
    {
        return 0;
    }
}

int main()
{
    cout << "Result of empty string: " << add("") << endl;
    return 0;
}